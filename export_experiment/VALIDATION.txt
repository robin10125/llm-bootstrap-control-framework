Bundle validation record
========================

The source and dependency versions were validated on 2026-07-13:

- All 28 tests under source/llm-framework/famework_testing/tests passed.
- verify_install.py passed imports, JAX device discovery, both MuJoCo XML/asset
  loads, and FFmpeg discovery.
- The hand-manipulation mock run completed successfully.
- JAX 0.10.2 detected CudaDevice(id=0) with the source machine's NVIDIA stack.
- The CPU and NVIDIA requirements resolved successfully from pip.

After replacing the architecture-specific wheelhouse, the destination-facing
shell scripts were syntax checked and the direct requirements were resolved in
a clean virtualenv. The final archive was integrity tested after creation.

The exact NVIDIA driver and transitive binary packages are deliberately chosen
on the destination and therefore cannot be validated for unknown hardware in
advance. Run verify_install.py after setup on that machine.

