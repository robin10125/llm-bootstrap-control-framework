FRAMEWORK (fixed): a STACKED phase gate blends the sub-priors for the phases [$phases] automatically
by observable signals (earlier phases when far from the goal, later phases as the task progresses).
You design one sub-prior per phase.
